{
  "name": "portfolio-monorepo",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "client",
    "server",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w @portfolio/frontend\" \"npm run dev -w @portfolio/backend\"",
    "build": "npx prisma generate --schema=packages/database/prisma/schema.prisma && npm run build -w @portfolio/shared && npm run build -w @portfolio/backend && npm run build -w @portfolio/frontend",
    "lint": "npm run lint --workspaces",
    "db:push": "npx prisma db push --schema=packages/database/prisma/schema.prisma",
    "db:seed": "node packages/database/prisma/seed.js"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "typescript": "^5"
  }
}